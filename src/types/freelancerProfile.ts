export type WorkspaceTheme = 'indigo' | 'sunset' | 'forest';

export type FreelancerProfile = {
  displayName: string;
  businessName: string;
  headline: string;
  city: string;
  website: string;
  whatsapp: string;
  bio: string;
  proposalSignature: string;
};
